# requirements.txt

# Processamento de PDF
pdfplumber==0.10.2       # Extração avançada de texto de PDFs
PyPDF2==3.0.1            # Alternativa para PDFs simples

# Processamento de texto e NLP
spacy==3.7.1             # Para extração de entidades (órgãos, cidades)
regex==2023.12.25        # Expressões regulares avançadas

# Manipulação de dados
pandas==2.1.4            # Criação da planilha
openpyxl==3.1.2          # Suporte para arquivos Excel (.xlsx)

# OCR para PDFs escaneados (imagens)
pytesseract==0.3.10      # Interface para Tesseract OCR
Pillow==10.2.0           # Processamento de imagens

# Utilitários
python-dotenv==1.0.0     # Carregar variáveis de ambiente (opcional)